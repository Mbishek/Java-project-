import org.springframework.stereotype.Component;

@Component
public class PayrollService {

    public double calculateSalary(double basic, double allowance) {
        return basic + allowance;
    }
}
